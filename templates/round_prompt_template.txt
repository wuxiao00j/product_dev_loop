你现在要执行一轮已经收口的小步开发任务。只有功能描述，不算合格 prompt；下面这些段落缺一项，都说明这轮还没定义清楚。请严格控制在本轮目标内，不要自行扩散。

【任务类型｜先选类型，再写下面内容】
- 本轮类型：`build / fix / polish / unblock`
- 选择原因：`一句话说明为什么这轮属于这个类型`

【当前项目信息｜先锁定项目和轮次，避免串上下文】
- 项目名：`<project-name>`
- 当前轮次：`round-0x`
- 当前阶段：`Build Delegation`
- 当前 PRD 状态：`draft / locked`
- 上轮结论 / 当前状态：`用一句话说明为什么进入本轮`

【执行前必读上下文｜先对齐现状，再开始实现】
- 必读：`PROJECT_STATE.md`
- 必读：`PRD.md`
- 必读：最近一轮 `PROGRESS.md`
- 必读：最近一轮 `IMPLEMENTATION_NOTES.md`
- 按需补读：`ARCHITECTURE.md`、`CHANGE_PROPOSALS.md`、`IMPLICIT_REQUIREMENTS.md`、`DECISIONS.md`、`TEST_RESULTS.md`
- 如果上下文冲突，先指出冲突，不要直接开做

【本轮目标｜只写这一轮要收掉的一件事】
- 本轮只解决：
- 这轮完成后，用户能直接看到的结果：
- 本轮目标来源：`PRD 条目 / 测试反馈 / 上轮未完成项`

【类型补强｜按类型补一句最关键说明】
- 如果是 `build`：本轮最小闭环 / 关键数据流
- 如果是 `fix`：复现现象 / 修复目标 / 回归风险
- 如果是 `polish`：可动范围 / 不可动范围
- 如果是 `unblock`：阻塞点 / 临时方案 / 正式方案

【本轮边界｜没有边界，就容易从修一处变成改一片】
- 必做：
- 不做：
- 保持不动：
- 重点文件 / 模块：

【验收标准｜用于判断这轮是否真的完成】
- 验收项 1：
- 验收项 2：
- 验收项 3：

【执行约束｜用于防止擅改范围和 PRD】
- 只处理本轮目标，不顺手补其他功能
- 不做无关重构，不改无关命名、目录结构或技术方案
- 如果 `PRD.md` 已 `locked`，不得直接改定义
- 如果发现新需求，只能提变更建议
- 如果发现隐性偏好，只能标记并请求确认，不能直接写进 PRD

【测试要求｜用于保证本轮结果可验证】
- 请返回测试入口、操作步骤、测试数据、预期结果、失败判定
- 如果本轮是修复类任务，请说明如何验证原问题已消失

【完成后必须结构化回传｜用于主 agent 快速收口】
请只返回结构化信息：

1. 已完成
2. 未完成
3. 关键代码信息
4. `stub / mock / 假数据 / 隐藏入口 / 未接线`
5. 风险与需确认点
6. 测试建议
7. PRD 对照

发出前提醒：
- 先过 `prompt_quality_checklist.md`
- 再按 `prompt_scoring_rubric.md` 打分
