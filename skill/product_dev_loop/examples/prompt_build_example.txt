你现在要执行 `todo-lite` 项目的 `round-04` 开发任务。本轮类型是 `build`，目标是补一个最小闭环，不要扩成功能包。

【当前项目信息】
- 项目名：`todo-lite`
- 当前轮次：`round-04`
- 当前阶段：`Build Delegation`
- 当前 PRD 状态：`locked`
- 上轮结论：`待办新增和列表展示已可用，但筛选入口尚未落地`

【执行前必读上下文】
- `PROJECT_STATE.md`
- `PRD.md`
- 最近一轮 `PROGRESS.md`
- 最近一轮 `IMPLEMENTATION_NOTES.md`

【本轮目标】
- 本轮只解决：补齐待办状态筛选的最小闭环
- 这轮完成后，用户可以直接看到的结果：在 `/todos` 页面切换“全部 / 未完成 / 已完成”时，列表结果立即按状态变化
- 本轮目标来源：`PRD.md` 中“按状态筛选待办”条目

【本轮边界】
- 必做：筛选控件、筛选状态切换、列表结果联动
- 不做：搜索、排序、批量操作、后端筛选接口
- 保持不动：新增待办流程、删除流程、页面结构
- 重点文件 / 模块：`src/pages/todos.tsx`、`src/components/todo-filter.tsx`

【类型补强】
- 本轮最小闭环：用户切换筛选条件后，当前列表立即反映状态变化
- 关键数据流：筛选控件 -> 当前筛选状态 -> 列表过滤结果 -> 页面展示

【验收标准】
- 验收项 1：默认进入 `/todos` 时显示“全部”
- 验收项 2：切换到“未完成”或“已完成”后，列表结果立即变化
- 验收项 3：本轮不改动搜索、排序和其他未在 PRD 中确认的能力

【测试要求】
- 请给出测试入口、操作步骤、测试数据、预期结果、失败判定

【完成后必须结构化回传】
1. 已完成
2. 未完成
3. 关键代码信息
4. `stub / mock / 假数据 / 隐藏入口 / 未接线`
5. 风险与需确认点
6. 测试建议
7. PRD 对照
