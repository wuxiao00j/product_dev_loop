你现在要执行 `todo-lite` 项目的 `round-03` 开发任务。本轮类型是 `build`，目标已经收口，请只处理这一轮确认的问题，不要顺手扩功能。只有功能描述不算合格 prompt，下面每一段都属于必需约束。

【任务类型】
- 本轮类型：`build`
- 选择原因：`当前主问题不是修 bug，而是把“去掉假数据并补齐空状态”这个最小闭环补完整`

【当前项目信息】
- 项目名：`todo-lite`
- 当前轮次：`round-03`
- 当前阶段：`Build Delegation`
- 当前 PRD 状态：`locked`
- 上轮结论：`Round 02 已完成新增待办与列表展示，但列表初始化仍带有假数据，空状态也未按 PRD 落地`

【执行前必读上下文】
- 必读：`PROJECT_STATE.md`
- 必读：`PRD.md`
- 必读：最近一轮 `PROGRESS.md`
- 必读：最近一轮 `IMPLEMENTATION_NOTES.md`
- 按需补读：`CHANGE_PROPOSALS.md`、`IMPLICIT_REQUIREMENTS.md`、`DECISIONS.md`
- 如果你发现上下文冲突，先指出冲突，不要直接改代码

【本轮目标】
- 本轮只解决：去掉待办列表初始化中的明显假数据，并补齐空状态展示
- 这轮完成后，用户可以直接看到的结果：首次进入 `/todos` 时，如果还没有待办，页面展示明确空状态；新增第一条待办后，列表展示真实新增结果，不再依赖示例假数据
- 本轮目标来源：`PRD.md` 中“待办列表展示”和“空列表可理解反馈”条目，以及 Round 02 回执中的“仍有假数据痕迹”遗留

【类型补强】
- 本轮最小闭环：空列表时能看懂当前状态；新增第一条待办后，列表展示真实结果
- 关键数据流：页面初始化状态 -> 空状态展示 / 新增待办 -> 列表结果更新

【本轮边界】
- 必做：
  - 去掉列表初始化中的示例假数据
  - 在 `/todos` 页面补齐明确空状态
  - 保持现有新增待办流程可继续使用
- 不做：
  - 删除待办
  - 登录
  - 分享
  - 多人协作
  - 后端持久化
- 重点文件 / 模块：`src/pages/todos.tsx`、`src/components/todo-list.tsx`

【验收标准】
- 验收项 1：首次进入 `/todos` 且没有待办时，页面显示明确空状态文案，不出现示例假数据
- 验收项 2：新增第一条待办后，页面展示真实新增结果，空状态消失
- 验收项 3：本轮不引入新的范围变化，不修改无关页面或功能

【执行约束】
- 只处理本轮目标，不顺手补其他功能
- 不做无关重构，不改无关命名或目录结构
- `PRD.md` 已 `locked`，不得擅改定义
- 如果发现新需求，只提出变更建议，不直接扩做
- 如果发现隐性要求或隐性偏好，先标记并请求确认，不直接写进 PRD

【测试要求】
- 完成后请给出测试入口、操作步骤、测试数据、预期结果、失败判定
- 请额外说明如何验证“假数据痕迹已消失，且空状态与新增流程没有明显回归”

【完成后必须结构化回传】
请只按下面结构返回：

1. 已完成
2. 未完成
3. 关键代码信息
4. `stub / mock / 假数据 / 隐藏入口 / 未接线`
5. 风险与需确认点
6. 测试建议
7. PRD 对照

【PRD 对照要求】
- 请说明本轮对应哪些 PRD 条目
- 请区分已满足和未满足项
- 请明确判断当前是否达到本轮验收标准
- 如果发现 PRD 与实际情况不一致，请单独列出变更建议，不要直接修改 PRD

【发出前检查】
- 先过 `prompt_quality_checklist.md`
- 再按 `prompt_scoring_rubric.md` 打分
