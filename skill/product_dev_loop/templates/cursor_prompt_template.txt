你现在要完成一个已经收口的小步开发任务。只有功能描述，不算合格 prompt；下面的结构同时定义了范围、验收和回传要求。请严格按下面要求执行，不要自行扩散。

【任务类型｜先锁定这轮是 build、fix、polish 还是 unblock】
- 本轮类型：`build / fix / polish / unblock`
- 选择原因：`一句话说明为什么这轮属于这个类型`

【当前项目信息｜用于锁定你在做哪个项目、哪一轮、哪个阶段】
- 项目：`<project-name>`
- 当前轮次：`round-0x`
- 当前阶段：`Build Delegation`
- PRD 状态：`draft / locked`
- 上轮结论 / 当前状态：`一句话说明进入本轮的原因`

【执行前必须先看上下文｜用于避免脱离项目现状直接开做】
- 必读：`PROJECT_STATE.md`
- 必读：`PRD.md`
- 必读：最近一轮 `PROGRESS.md`
- 必读：最近一轮 `IMPLEMENTATION_NOTES.md`
- 按需补读：`ARCHITECTURE.md`、`CHANGE_PROPOSALS.md`、`IMPLICIT_REQUIREMENTS.md`、`DECISIONS.md`、`TEST_RESULTS.md`
- 如果上下文互相冲突，先指出冲突，不要直接开做

【本轮目标｜只写这一轮必须收掉的一个小目标】
- 本轮只解决：
- 这轮完成后，用户可以直接看到的结果：
- 本轮目标来源：`PRD 条目 / 测试反馈 / 上轮未完成项`

【类型补强｜按任务类型补一句关键约束】
- 如果是 `build`：请写本轮最小闭环和关键数据流
- 如果是 `fix`：请写复现现象、修复目标和回归风险
- 如果是 `polish`：请写可动范围和不可动范围
- 如果是 `unblock`：请写阻塞点，以及临时方案 / 正式方案

【本轮边界｜用于防止顺手扩功能】
- 必做：
- 不做：
- 保持不动：
- 重点修改文件：
- 可参考文件：

【验收标准｜用于定义怎样才算这轮完成】
- 验收项 1：
- 验收项 2：
- 验收项 3：

【硬约束｜用于防止擅改 PRD、方案或范围】
- 只实现当前目标，不顺手扩展新功能
- 必须遵守现有 PRD 和架构说明
- 如果发现需求与 PRD 不一致，只能提出变更建议，不能直接修改定义
- 如果发现新的隐性要求或隐性偏好，先标记，不要擅自写进 PRD
- 优先改动已有文件；如果必须新增文件，要说明原因
- 不做无关重构，不补做未被要求的页面、接口或配置
- 不擅自修改命名、目录结构或技术方案
- 不要为了“更完整”而超出本轮范围

【测试要求｜用于让结果可直接转入测试阶段】
- 请返回测试入口
- 请返回操作步骤
- 请返回测试数据
- 请返回预期结果
- 请返回失败判定
- 如果本轮是修复类任务，请说明如何验证原问题已消失且没有明显回归

【完成后必须返回的信息｜请按结构化格式回传，不要粘贴大段原始过程】
请只返回结构化结果，按下面结构返回：

1. 已完成
- 这轮实际完成了什么
- 对应满足了哪些验收项

2. 未完成
- 还有哪些目标没完成
- 为什么没完成

3. 关键代码信息
- 改了哪些关键文件
- 每个关键文件负责什么
- 关键函数、组件、接口或逻辑点是什么

4. 特殊状态检查
- 哪些地方仍然是 `stub`
- 哪些地方仍然是 `mock`
- 哪些地方仍然是 `假数据`
- 哪些入口是 `隐藏入口`
- 哪些逻辑还 `未接线`

5. 风险与变更建议
- 当前已知风险
- 需要用户确认的点
- 如果发现 PRD 与实际需求不一致，请单独列出变更建议，不要直接改定义

6. 测试建议
- 测试入口
- 操作步骤
- 测试数据
- 预期结果
- 失败判定

7. PRD 对照
- 本轮对应哪些 PRD 条目
- 已满足的 PRD 条目
- 未满足的 PRD 条目
- 当前是否达到本轮验收标准

发出前提醒：
- 先过 `prompt_quality_checklist.md`
- 再按 `prompt_scoring_rubric.md` 打分
