你现在拿到的不是现成可发的 round prompt，而是一段原始需求草稿，并且当前 `Prompt Gate` 结果应视为 `REWRITE_REQUIRED`。不要把草稿原样派给 Cursor、Claude Code 或 Codex。先识别缺陷，再把它收口成一个可控、可验收、可回传的 round prompt。

目标不是保留草稿里的所有愿望，而是把这一轮压成一个最小可执行闭环。需求写得很长，也不等于已经收口。

注意：

- rewrite 完成后，必须重新过 `Prompt Gate`
- 未重新拿到 `PASS` 前，不得进入 `Build Delegation`

【输入草稿】
`<raw-request>`

【先做收口判断｜这一步不要省】
- 先判断这段输入为什么仍属于“原始需求草稿”
- 先列出不能直接派单的关键缺陷
- 如果草稿同时包含多个目标，先拆分，只保留本轮最低闭环
- 如果项目上下文不清楚，先补齐项目名、轮次、阶段、PRD 状态；补不齐时，不要硬派单
- 如果 `PRD.md` 已 `locked` 且草稿包含新需求或范围变化，不得直接扩做；应转成变更建议，或把本轮目标收缩到不改 PRD 的部分
- 如果存在接口、权限、环境、数据源、第三方服务等不确定依赖，必须写明降级规则或阻塞规则

【输出格式｜默认输出两部分】
第一部分：草稿缺陷摘要
- 草稿类型：`功能清单 / issue 列表 / 用户口述需求块 / 多模块打包需求 / 带文件路径但缺边界 / 带命令 API UI 要求但没有验收和回传 / 其他`
- 不能直接派单的原因：
- 本轮收口策略：
- 被延后到后续轮次或变更提议的内容：

第二部分：收口后的 round prompt

【收口后的 round prompt 必须包含以下段落】

【当前项目信息】
- 项目名：
- 当前轮次：
- 当前阶段：`Build Delegation`
- 当前 PRD 状态：`draft / locked`
- 上轮结论 / 当前状态：

【任务类型】
- 本轮类型：`build / fix / polish / unblock`
- 选择原因：

【执行前必读上下文】
- 必读：`PROJECT_STATE.md`
- 必读：`PRD.md`
- 必读：最近一轮 `PROGRESS.md`
- 必读：最近一轮 `IMPLEMENTATION_NOTES.md`
- 按需补读：
- 如果上下文冲突，先指出冲突，不要直接开做

【本轮目标】
- 本轮只解决：
- 这轮完成后，用户能直接看到的结果：
- 本轮目标来源：`PRD 条目 / 测试反馈 / 上轮未完成项 / 外部阻塞`

【类型补强】
- 如果是 `build`：写本轮最小闭环和关键数据流
- 如果是 `fix`：写复现现象、修复目标、回归风险
- 如果是 `polish`：写可动范围、不可动范围
- 如果是 `unblock`：写阻塞点、解阻目标、临时方案 / 正式方案

【本轮边界】
- 必做：
- 不做：
- 保持不动：
- 重点文件 / 模块：

【验收标准】
- 验收项 1：
- 验收项 2：
- 验收项 3：

【依赖与降级规则】
- 关键依赖：
- 依赖满足时：
- 依赖不满足时允许的降级方案：
- 哪些情况应直接标记阻塞，不继续扩做：

【执行约束】
- 只处理本轮目标，不顺手补其他功能
- 不做无关重构，不改无关命名、目录结构或技术方案
- 如果 `PRD.md` 已 `locked`，不得擅改定义
- 如果发现新需求，只提出变更建议，不直接扩做
- 如果发现隐性要求或隐性偏好，先标记并请求确认，不直接写进 PRD

【测试要求】
- 请返回测试入口
- 请返回操作步骤
- 请返回测试数据
- 请返回预期结果
- 请返回失败判定
- 如果本轮是修复类或解阻类任务，请说明如何验证原问题已消失，或阻塞已解除到可继续推进

【完成后必须结构化回传】
请只按下面结构返回：

1. 已完成
2. 未完成
3. 关键代码信息
4. `stub / mock / 假数据 / 隐藏入口 / 未接线`
5. 风险与需确认点
6. 测试建议
7. PRD 对照

【PRD 对照要求】
- 请说明本轮对应哪些 PRD 条目
- 请区分已满足和未满足项
- 请明确判断当前是否达到本轮验收标准
- 如果发现 PRD 与现实不一致，请列成变更建议，不要直接修改 PRD

【发出前检查】
- 先重新过 `Prompt Gate`
- 先过 `docs/raw_prompt_defect_checklist.md`
- 再过 `templates/prompt_quality_checklist.md`
- 再按 `docs/prompt_scoring_rubric.md` 打分
- 低于最低标准时，不要直接发
