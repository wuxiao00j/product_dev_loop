你现在要执行 `todo-lite` 项目的 `round-05` 开发任务。本轮类型是 `fix`，只修复已经明确复现的问题，不扩新功能。

【当前项目信息】
- 项目名：`todo-lite`
- 当前轮次：`round-05`
- 当前阶段：`Build Delegation`
- 当前 PRD 状态：`locked`
- 上轮结论：`用户测试发现连续快速提交两次时会新增重复待办`

【执行前必读上下文】
- `PROJECT_STATE.md`
- `PRD.md`
- 最近一轮 `PROGRESS.md`
- 最近一轮 `IMPLEMENTATION_NOTES.md`
- `TEST_RESULTS.md`

【问题现象与本轮目标】
- 复现现象：在 `/todos` 页面连续快速点击提交按钮两次，会生成两条相同待办
- 本轮只解决：修复重复提交导致的重复创建问题
- 修复后用户可见结果：连续点击提交时不会生成重复项，且原有正常新增流程不受影响

【本轮边界】
- 必做：修复重复提交、保住现有新增主路径
- 不做：删除待办、筛选、文案优化、页面重构
- 保持不动：列表展示结构、已有空状态逻辑
- 重点文件 / 模块：`src/components/todo-form.tsx`

【类型补强】
- 回归风险：不要把正常提交也一起锁死
- 修后验证：请说明如何验证“原问题已消失，且正常新增仍可用”

【验收标准】
- 验收项 1：连续快速点击提交，不会生成重复待办
- 验收项 2：正常单次提交仍能成功新增
- 验收项 3：本轮不引入新的表单行为变化

【测试要求】
- 请给出复现步骤、修后验证步骤、预期结果和失败判定

【完成后必须结构化回传】
1. 已完成
2. 未完成
3. 关键代码信息
4. `stub / mock / 假数据 / 隐藏入口 / 未接线`
5. 风险与需确认点
6. 测试建议
7. PRD 对照
