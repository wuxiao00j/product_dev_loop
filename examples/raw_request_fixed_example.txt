你现在要执行 `ops-console` 项目的 `round-06` 开发任务。本输入已通过 `Prompt Gate = PASS`，本轮类型是 `build`，目标已经收口。用户给过一段较大的功能草稿，但这轮只处理“工单列表真实数据接线 + 空状态 / 错误状态闭环”，不要把详情、导出和权限补齐一起混进来。

【任务类型】
- 本轮类型：`build`
- 选择原因：`当前最阻碍闭环的不是新增更多功能，而是让工单列表从真实数据到结果展示先走通`

【当前项目信息】
- 项目名：`ops-console`
- 当前轮次：`round-06`
- 当前阶段：`Build Delegation`
- 当前 PRD 状态：`locked`
- 上轮结论：`Round 05 已完成工单列表基础 UI，但当前仍依赖示例数据，空状态和报错态未按 PRD 落地`

【执行前必读上下文】
- 必读：`PROJECT_STATE.md`
- 必读：`PRD.md`
- 必读：最近一轮 `PROGRESS.md`
- 必读：最近一轮 `IMPLEMENTATION_NOTES.md`
- 按需补读：`ARCHITECTURE.md`、`CHANGE_PROPOSALS.md`、`DECISIONS.md`、`TEST_RESULTS.md`
- 如果上下文冲突，先指出冲突，不要直接开做

【本轮目标】
- 本轮只解决：把工单列表页接到现有真实列表数据来源，并补齐列表页的空状态、loading 和错误状态
- 这轮完成后，用户能直接看到的结果：进入工单列表页后，不再依赖示例假数据；有数据时展示真实列表，无数据时展示明确空状态，请求失败时展示可理解错误状态
- 本轮目标来源：`PRD.md` 中“工单列表主路径可用”条目，以及 Round 05 回执中的“仍有 mock 数据痕迹、状态页未补齐”遗留

【类型补强】
- 本轮最小闭环：列表页能从真实数据请求到结果展示形成闭环
- 关键数据流：列表页进入 -> 请求真实列表数据 -> 成功展示列表 / 无数据展示空状态 / 失败展示错误状态

【本轮边界】
- 必做：
  - 去掉工单列表的示例假数据依赖
  - 接好现有工单列表数据请求链路
  - 补齐列表页空状态、loading、错误状态
- 不做：
  - 工单详情页
  - 导出 CSV
  - 权限体系补齐
  - 搜索和筛选增强
  - 新增后端接口设计
- 保持不动：
  - 现有详情页路由结构
  - 非工单模块页面
  - 无关目录和命名整理
- 重点文件 / 模块：`src/pages/tickets/index.tsx`、`src/components/ticket-list*`、现有工单列表数据访问层

【验收标准】
- 验收项 1：进入工单列表页时，不再展示示例假数据，而是展示真实列表结果或明确空状态
- 验收项 2：列表请求进行中时展示 loading 状态；请求失败时展示可理解错误状态，不是空白页
- 验收项 3：本轮不扩做详情、导出、权限或额外接口改造

【依赖与降级规则】
- 关键依赖：现有工单列表接口或现有数据访问层必须能返回真实列表结果
- 依赖满足时：按现有数据链路完成列表页接线和状态展示
- 依赖不满足时允许的降级方案：仅允许把失败状态和缺失依赖明确暴露出来，并在回传里标记阻塞；不要自行发明新的正式接口或扩做详情能力
- 哪些情况应直接标记阻塞，不继续扩做：如果现有列表数据源不可用、接口契约缺失或权限前置条件缺失，直接标记阻塞并说明缺什么

【执行约束】
- 只处理本轮目标，不顺手补其他功能
- 不做无关重构，不改无关命名、目录结构或技术方案
- `PRD.md` 已 `locked`，不得擅改定义
- 如果发现新需求，只提出变更建议，不直接扩做
- 如果发现隐性要求或隐性偏好，先标记并请求确认，不直接写进 PRD

【PASS Evidence】
- Prompt Gate：`PASS`
- Round main closure：`工单列表从真实数据请求到列表 / 空状态 / 错误状态展示形成一个单轮主闭环`
- Why not split：`本轮只有一个主闭环；空状态、loading、错误状态只是同一列表主路径的必要接线，不是独立闭环`
- Fallback / blocker rule：`如果现有列表数据源不可用或接口契约缺失，直接标记阻塞，不扩做详情、导出或新接口设计`
- Delegation contract：`satisfied`

【测试要求】
- 请返回测试入口、操作步骤、测试数据、预期结果、失败判定
- 请额外说明如何验证“真实列表数据已接上、空状态 / loading / 错误状态已补齐，且没有把详情、导出、权限范围混进本轮”

【完成后必须结构化回传】
请只按下面结构返回：

1. 已完成
2. 未完成
3. 关键代码信息
4. `stub / mock / 假数据 / 隐藏入口 / 未接线`
5. 风险与需确认点
6. 测试建议
7. PRD 对照

【PRD 对照要求】
- 请说明本轮对应哪些 PRD 条目
- 请区分已满足和未满足项
- 请明确判断当前是否达到本轮验收标准
- 如果发现 PRD 与现实不一致，请列成变更建议，不要直接修改 PRD

【发出前检查】
- 先过 `docs/raw_prompt_defect_checklist.md`
- 再过 `templates/prompt_quality_checklist.md`
- 再按 `docs/prompt_scoring_rubric.md` 打分
